{
  "name": "sender",
  "version": "1.0.0",
  "description": "Validates a single report chunk from S3 before it is sent for downstream processing.",
  "main": "index.js",
  "scripts": {
    "test": "jest index.test.js"
  },
  "license": "ISC",
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.701.0",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "JSONStream": "^1.3.5"
  }
}
